# produtox-landing
git init
git add .
git commit -m "Landing page ProdutoX"
# substitua SEU_USUARIO pelo seu usuário no GitHub e PRODUTOREPO pelo nome do repo criado
git remote add origin git@github.com:SEU_USUARIO/produtox-landing.git
git branch -M main
git push -u origin main
